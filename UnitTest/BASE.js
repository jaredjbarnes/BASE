BASE.require(["BASE.util.UnitTest"], function () {

    BASE.namespace("UnitTest");

    UnitTest.BASE = (function (Super) {

        var _BASE = function () {

        };

        BASE.extend(_BASE, Super);

        return _BASE;

    }(BASE.util.UnitTest));

});
